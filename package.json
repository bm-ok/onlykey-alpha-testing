{
  "name": "onlykey-testing",
  "version": "0.1.0",
  "private": true,
  "description": "Automated Mocha test suite for OnlyKey firmware + CLI + web app, driving the physical device over the DEBUG-serial button-injection channel.",
  "type": "commonjs",
  "scripts": {
    "test": "mocha"
  },
  "dependencies": {
    "node-hid": "^3.4.0"
  },
  "devDependencies": {
    "mocha": "^10.7.3"
  }
}
